spring:
  jpa:
    database: mysql
  datasource:
    name: mysql
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      url: jdbc:mysql://127.0.0.1:3306/springboot_study?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true
      username: root
      password: 123456

# MyBatis
mybatis:
  mapper-locations: classpath:mappers/*.xml  #注意：一定要对应mapper映射xml文件的所在路径
#  type-aliases-package: com.talkingdata.service.entity  # 注意：对应实体类的路径

logging:
  level:
    cn:
      interhorse:
        springboot:
          mybatisdruidmysql:
            dao:
              mapper: debug